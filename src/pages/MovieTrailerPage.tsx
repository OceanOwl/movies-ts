import React from 'react';
import {MovieTrailer} from "../components/MovieTrailer/MovieTrailer";

const MovieTrailerPage = () => {
    return (
        <div>
            <MovieTrailer/>
        </div>
    );
};

export {MovieTrailerPage};